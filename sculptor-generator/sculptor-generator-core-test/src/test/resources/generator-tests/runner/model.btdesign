Application Test {
    basePackage=com.acme

	Module library {
        Entity Media {

            Repository MediaRepository {
            	"*** This finder method has a missing 'gap' keyword ***"
                List<@Media> findMediaByName(IDTYPE libraryId, String name);
            }
        }
    }
}
